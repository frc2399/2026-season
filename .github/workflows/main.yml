# This is a basic workflow to build robot code.
name: CI
# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the main branch.
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-22.04
    # This grabs the WPILib docker container
    container: wpilib/roborio-cross-ubuntu:2025-22.04
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v4
    # Declares the repository safe and not under dubious ownership.
    - name: Add repository to git safe directories
      run: git config --global --add safe.directory $GITHUB_WORKSPACE
    # Grant execute permission for gradlew
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    # Runs a single command using the runners shell
    - name: Compile and run tests on robot code
      run: ./gradlew build
      
  checkForBuildConstantsFile:
    runs-on: ubuntu-22.04
    container: wpilib/roborio-cross-ubuntu:2025-22.04
    steps:
      - uses: actions/checkout@v4  
      - name: Add repository to safe directories
        run: git config --global --add safe.directory $GITHUB_WORKSPACE
      - name: Make sure the BuildConstants file is not included in PR
        run: |
          PROHIBITED_FILE="src/main/java/frc/robot/BuildConstants.java"
          if [ -f "$PROHIBITED_FILE" ]; then
            echo "::error::Prohibited file '$PROHIBITED_FILE' is included in the repository. Run this to remove it: git rm -r src/main/java/frc/robot/BuildConstants.java."
            exit 1
          else
            echo "Prohibited file '$PROHIBITED_FILE' not found. Continuing workflow."
          fi
